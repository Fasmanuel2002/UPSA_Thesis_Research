version: "3.9"

services:
  app:
    build: .
    image: upsa-thesis-research:latest
    container_name: upsa_thesis_app
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - PYTHONPATH=/app
    stdin_open: true
    tty: true
    command: bash
